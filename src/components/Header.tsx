import './Header.css'   //Load the css file called Header.css that is in the same folder as the one i am right now (header.tsx)

function Header() {
    return (
        <header className = "header">
            <h1>KnowYourWings</h1>
        </header>
    )
}

export default Header